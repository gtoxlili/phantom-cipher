# inf-fingerprint 限流豁免 key。
#
# 浏览器场景：build 完后会被 inline 进 dist 的 JS（任何前端 secret 都
# 是这个命运），所以只要 build 出来上线了、它就在公网上能看见。这层
# 注入主要是为了**仓库代码不带 key**——clone 仓库的人看不到。
#
# 用法：
#   - 本地开发：复制为 .env.local 并填上 key（.env.local 已 gitignored）
#   - CI：GitHub Secrets 里设 INF_FP_API_KEY，
#     workflow 把它作为 docker build-arg 透传给 Dockerfile，
#     Dockerfile 里用 ARG + ENV 转给 vite build，
#     vite 检测到 VITE_ 前缀的环境变量自动 inline 到 import.meta.env
VITE_INF_FP_API_KEY=
